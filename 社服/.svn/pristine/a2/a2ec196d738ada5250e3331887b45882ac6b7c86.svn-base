//index.js
//获取应用实例
const app = getApp()
Page({
  data: {
    info:{},
  },
  onLoad: function () {
    this.getInfo();
  },
  getInfo(){
    wx.request({
      url: app.globalData.url.INDEX,
      success: res => {
        let { code, data } = res.data.data;
        if( code === 200 ){
          this.setData({ info: data,  })
        }
        console.log('get info ad', data )
      }
    })
  },
  tel( e ){
    wx.makePhoneCall({
      phoneNumber: e.currentTarget.dataset.tel
    })
  }
})
